{$DOMAIN}

encode gzip zstd

handle /api/* {
	reverse_proxy backend:5001
}

# Only used when USE_LOCAL_STORAGE=true (Cloudinary serves uploads directly otherwise)
handle /uploads/* {
	reverse_proxy backend:5001
}

handle {
	reverse_proxy frontend:80
}
